{% import '_includes/forms' as forms %}

{{ forms.textField({
    first:        true,
    label:        'Thumbnails cache path'|t,
    id:           'cachePath',
    name:         'cachePath',
    instructions: 'Path relative to document root where preview thumbnails will be stored.'|t,
    value:        settings.cachePath,
    autofocus:    true,
    errors:       settings.getErrors('cachePath')
}) }}